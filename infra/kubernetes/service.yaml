apiVersion: v1
kind: Service
metadata:
  name: agentic-api
  namespace: agentic
  labels:
    app: agentic-api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: agentic-api
